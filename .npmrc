# Keep npm's cache inside the workspace so installs work under a confined file sandbox
# and so the project never writes outside its own directory.
cache=./.npm-cache
fund=false
audit=false
save-exact=false
